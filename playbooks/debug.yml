# This can be used to test that ansible is setup correctly, that hosts are reachable and to gather facts about them
- hosts: all

  tasks:
    - name: Useful task
      debaug:
        msg: Hello world

